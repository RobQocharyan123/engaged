# Use the deployed API URL when the frontend and backend are hosted separately.
# Leave unset only when the host proxies /api to engagedNode.
REACT_APP_API_BASE_URL=https://your-api.example.com/api
REACT_APP_INVITATION_ID=289
